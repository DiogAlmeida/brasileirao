import { Identifiable } from '..';

export interface ILeaderboards extends Identifiable {

  name: string,
  totalPoints: number,
  totalGames: number,
  totalVictories: number,
  totalDraws: number,
  totalLosses: number,
  goalsFavor: number,
  goalsOwn: number,
}
